/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'images.pexels.com',
            'example.com',
            'media.istockphoto.com',
        ],
    },
};

module.exports = nextConfig;
